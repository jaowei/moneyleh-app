services:
  app:
    build: .
    restart: unless-stopped
    ports: 
      - "9000:9000"
    volumes:
      - moneyleh-app-data:/data
    environment:
      - BETTER_AUTH_SECRET
      - BETTER_AUTH_URL

volumes:
  moneyleh-app-data:
